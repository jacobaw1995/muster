VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Phase 14 — Cloudflare Turnstile site key (public). Event creation works
# without it (the create-event Edge Function skips verification when its
# own TURNSTILE_SECRET_KEY is also unset), but is unprotected until both
# are set. See SETUP.md Director steps.
VITE_TURNSTILE_SITE_KEY=
