# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvb
*.njsproj
*.sln
*.sw?

# PWA generated files
dev-dist

# Claude Code local-only settings (machine/session-specific permissions —
# .claude/launch.json IS committed, it's the shared dev-server config)
.claude/settings.local.json

# Supabase CLI local cache (machine-specific, e.g. version-check state) —
# not project content, never has secrets, but shouldn't be committed
supabase/.temp/
