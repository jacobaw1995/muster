# Muster

Mobile-first PWA events + impact app for the Operator Standard community.

See [SETUP.md](./SETUP.md) for how to run this project and what's still outstanding.

The authoritative design spec lives in [`design_handoff_muster_events_app/README.md`](./design_handoff_muster_events_app/README.md); the pixel reference is the `.dc.html` file in that same folder (open it directly in a browser — it does not run through this app's build).
